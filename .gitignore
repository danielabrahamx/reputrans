node_modules/

# Environment variables — NEVER commit secrets
.env
.env.local
.env.production
.env.staging
.env.*.local
# .env.example is safe to commit (contains no secrets, just template docs)
!.env.example

target/
*.lock
dist/
conversation.md

# Build outputs
.next/
out/
.netlify/
